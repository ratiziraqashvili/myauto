import { auth } from "@/auth";

const AddPage = async () => {
    const session = await auth();

    return ( 
        <div className="">
            fsfsf
        </div>
     );
}
 
export default AddPage;